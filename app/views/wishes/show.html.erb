<h1><%= @wish.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @wish.body %></p>
  </div>
  <div class="col-md-4">
    <%= link_to "Edit", edit_wish_path(@wish), class: 'btn btn-success' %>
  </div>
</div>
