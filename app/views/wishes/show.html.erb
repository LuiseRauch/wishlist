<h1><%= @wish.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @wish.body %></p>
  </div>
  <div class="col-md-4">
    <%= link_to "Edit", edit_list_wish_path(@wish.list, @wish), class: 'btn btn-success' %>
    <%= link_to "Delete Wish", [@wish.list, @wish], method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this wish?' } %>
  </div>
</div>
